const rgb2yuv = ({r, g, b}) => {
  return {
    y: 16 + (.003906 * ((65.738 * r) + (129.057 * g) + (25.064 * b))),
    u: 128 + (.003906 * ((112.439 * r) + (-94.154 * g) + (-18.285 * b))),
    v: 128 + (.003906 * ((-37.945 * r) + (-74.494 * g) + (112.439 * b)))
  }
};

module.exports = rgb2yuv;
